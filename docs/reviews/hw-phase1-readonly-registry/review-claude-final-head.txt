Verdict: APPROVED

Findings:
- `history.ts`: Local fallback now runs in both the backend-unavailable branch and after a successful but empty backend response, using a single `getLocalHistoryPayload(sessionKey)` helper that reuses the exported `buildLocalHistoryPayload`. The early read of `limit`/`rawSessionKey`/`friendlyId` no longer races with auth/probe — auth runs first, then params are parsed. Tests cover the shape of the local payload (`source: 'local'`, content wrapped as `[{type:'text', text}]`, `historyIndex` populated).
- `sessions.ts`: `buildUnavailableSessionsPayload` now emits real local sessions (via `listLocalSessions()`) instead of an empty array in the no-sessions-capability path, with `source` toggling between `'local'` (non-empty) and `'unavailable'` (empty). Summary fields (`key`, `friendlyId`, `derivedTitle`, `message_count`) match what the existing dashboard consumes.
- `vector-memory/discovery.ts`: Endpoint hard-disabled with a 405 + read-only message. Note (non-blocking): the auth check was removed along with the body parsing. Functionally fine since the handler is a constant 405, but a normally-authenticated dashboard endpoint silently dropping its `isAuthenticated` gate is worth a follow-up if the team wants uniform auth telemetry on all `/api/*` routes.
- `vector-memory.ts` / `vector-memory.test.ts`: `storeTeamDiscovery`, `upsertDocument`, `teamKnowledgeCollection`, and the `crypto` import are fully removed, and the negative test asserts `'storeTeamDiscovery' in mod === false`, locking in the read-only surface. `VectorMemoryInputError` and `StoreTeamDiscoveryResult` were both write-only — `StoreTeamDiscoveryResult` is gone; `VectorMemoryInputError` remains exported but is no longer thrown anywhere in this module. Non-blocking: consider removing it in a later cleanup if no external caller depends on the symbol.
- `vector-memory-screen.tsx`: Discovery textarea, character counter, and `storeDiscoveryMutation` are removed; replacement copy clearly states the read-only phase and where authority lives (source repos + GitHub). Selection-from-store side effect is gone, so `setSelected` is no longer driven by writes — consistent with read-only behavior.
- `swarm2-screen.tsx`: `mergeRegistryRosterWithCrew` now synthesizes registry-only members as `gatewayState: 'offline'`, `processAlive: false`, `provider: 'registry'`, with zeroed counters and preserved `skills`/`capabilities` from the registry entry. They're appended pre-sort so room ordering still applies. The updated test asserts exactly that shape and that live crew entries still take precedence by id.
- `swarm2-screen.tsx` toast calls: migrated from object form (`{title, description, variant}`) to positional `toast(message, {type})`. Test suite passes per controller, so the call sites now match the project's toast API.
- `RuntimeEntry` gains `lastRealSummary` / `lastRealResult` typed as `string | null | undefined`. I can only see the type addition in the diff; if these aren't yet read in `displayTaskTitle` or related render paths, they're harmless dead fields, but worth confirming a follow-up commit wires them up.
- `git diff --check` clean and the four targeted vitest files green (23 tests) per the controller summary.

No correctness, security, or regression concerns that block merge.
